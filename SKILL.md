---
name: 小红书虚拟选品诊断
description: Use when a Xiaohongshu merchant needs to diagnose whether a virtual product / digital good is worth making and listing — given a product link, title, price, sales, or screenshot. Outputs a 推荐做 / 改造后再做 / 不建议做 verdict plus adaptation directions and a standalone HTML diagnosis report.
---

# 小红书虚拟选品诊断

## Overview

帮小红书商家判断一个**虚拟商品 / 数字商品**到底值不值得做、怎么把参考品改造成自己的产品、上架前要规避哪些风险。最终交付物是一份可直接打开的独立 HTML 选品诊断报告。

这不是商品库查重，也不是通用文案生成。核心价值是**商家决策支持**：选品、改造、风险、下一步动作。

## Input Modes

支持两种输入：商品链接，或手动粘贴的商品信息。

### 商品链接

如果用户给的是小红书商品详情链接，在可访问时读取公开商品信息。读不到时，只向用户索取最少必要字段，而不是卡在可选数据上。

不要仅凭一个商品链接就去自动搜索或匹配本地选品库 CSV、历史导出、Downloads 文件或既往会话的数据集。只有当用户明确给出该文件、或明确说「用选品库来评估」时，才使用外部 / 本地选品库数据。

对于小红书页面，优先从渲染后的商品详情页直接抓取关键信息：可见 DOM、页面状态、网络层可见数据，必要时用截图提取。抓取商品事实：标题、价格、原价、销量、店铺名、店铺评分、粉丝数、店铺已售、主图文案、保障 / 发货说明、详情页可见卖点。DOM 文本或无障碍快照可能为空，即使页面视觉已加载，所以截图提取是兜底手段，不是首选。

### 手动商品信息

基础判断的必填字段：

- 商品名称
- 商品类目
- 商品价格
- 商品销量

强化判断的可选字段：

- 商品链接
- 商品主图或封面截图
- 详情页文案
- 交付方式
- 目标人群

不要向普通商家索要后台型字段，如：是否已下架、平台提示原因、近 7 天销量增长、近 30 天销量增长、搜索关键词——除非他们已经提供。

## Scope

只评估用户提供的商品详情页与商品信息。不要去采集达人主页、主页笔记卡片、单篇笔记链接、评论或笔记级内容。如果用户给了账号或笔记链接，除非用户明确要求在本 Skill 之外做一次独立的内容推广分析，否则忽略。

## Diagnosis Workflow

1. 提取已有的商品事实，区分「观察到的事实」与「推断」。
2. 若最少必要信息缺失且无法读取，只追问缺失的必填字段。
3. 先下商家决策判断：**推荐做 / 改造后再做 / 不建议做**。
4. 用商家能听懂的话解释判断：需求、销量、价格、竞争、交付难度、平台风险。
5. 拆解参考品为什么卖得动：用户痛点、承诺、价格带、包装、大概率是交付什么。
6. 指出哪些绝对不能照搬：版权素材、课程 / 资料搬运、题库 / 答案、软件授权、AI / PS / 字体素材、过度承诺、自动发货纠纷、敏感词、资质问题。
7. 给出 3 个商家自己能做出来的改造方向，而不是抄版。
8. 给上架包装建议：标题方向、封面卖点、套餐结构、详情页大纲、发货说明、售后说明。
9. 在本地可写文件时，生成一份独立 HTML 报告并保存为用户交付物。

用浏览器采集商品页时，聚焦商品详情页本身。不要在本 Skill 内打开账号页或笔记页。

## Scoring

用 1–5 分，方向清晰：机会类分数越高越好；风险与难度类分数越高越差。

- 需求强度
- 增长潜力：仅当用户提供了近期趋势字段（近 7 天销量增长、近 30 天销量增长，或已有的日销追踪数据）时才打分。没有趋势数据就直接省略该项，不要显示「信息不足」「--」或占位卡。
- 价格合理度
- 差异化空间
- 交付难度
- 违规风险

不要把平台风险分析包装成官方小红书规则解读。明确说明这是基于样本与内容风险的判断。

## HTML Report Contract

生成一份独立 `.html` 文件，命名与标题都作为选品诊断产物。不要用「诊断报告」作为报告名，优先用「小红书虚拟选品诊断」。加一句副标题，说明本分析参考了小红书近期热销虚拟商品与平台下架违规样本。使用可读的中文文案、干净的版面，除非用户明确要求，否则不依赖任何外部资源。内联足够 CSS，使报告能直接用浏览器打开。

必含章节：

1. `最终判断`：推荐做 / 改造后再做 / 不建议做，加一句话。
2. `商品基础信息`：名称、类目、价格、销量、来源类型，以及任何用户提供的图片 / 链接。
3. `机会评分`：评分表或卡片。
4. `为什么这么判断`：务实的解释。
5. `爆品拆解`：用户为什么买，参考品到底在卖什么。
6. `不能照搬的地方`：风险与不可复制部分。
7. `怎么改成自己的产品`：3 个具体改造方向。
8. `上架包装建议`：标题、封面、套餐、详情页、发货、售后。
9. `上架前风险检查清单`：标题、详情页、交付物、版权 / 授权、承诺措辞、售后。
10. `下一步动作`：商家今天就能做的 5–8 个动作。

可选：在 `references/report_template.html` 的基础上套用样式，保证多份报告视觉一致。

## Output Rules

- 最终回复里先放 HTML 报告链接。
- 简要说明用了哪种输入层级：商品链接采集，还是手动商品信息。
- 简要说明判断仅基于商品详情信息。
- 只有在缺失字段实质影响置信度时才提及。
- 用户要求本 Skill 常规报告时，不要只输出 Markdown。
- 不要为了报告去索取或采集账号主页、笔记链接、趋势字段、下架状态、平台提示、搜索关键词。
- 不要静默地把本地选品库 CSV 行混入商品链接诊断。公开商品页读不到时，改为索取商品名称、商品类目、商品价格、商品销量。
- 在小红书页面上不要停在空的 DOM 快照。先尝试截图可见提取，再判断必填字段是否仍缺失。
